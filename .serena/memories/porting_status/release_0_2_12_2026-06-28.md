# 2026-06-28: version bump to 0.2.12

- Bumped package version from `0.2.11` to `0.2.12` in `Cargo.toml`.
- Ran `cargo check`, which updated the root `narou_rs` package entry in `Cargo.lock` to `0.2.12` and completed successfully.
- No release tag was created because the user requested a version bump process but did not explicitly specify a tag/version number for tagging.
- This version includes the n3352gq illustration deduplication work, Web ebook streaming/list state persistence, hash-backed illustration store, mitemin filename compatibility, and raw-backed mitemin filename migration.
